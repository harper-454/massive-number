import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MASSIVE NUMBER — The AI Coding Platform",
  description: "The most powerful AI coding platform. Multi-model orchestration, real-time web grounding, visual agent pipelines, cost optimization. Beats Cursor, Windsurf, Kiro, and VS Code combined.",
  keywords: ["MASSIVE NUMBER", "AI coding", "vibe coding", "multi-model", "agent mode", "Cursor alternative", "Windsurf alternative", "Kiro alternative"],
  authors: [{ name: "MASSIVE NUMBER" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
